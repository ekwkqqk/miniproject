<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miniproject.mail.domain.MailTemplateRepository">

    <resultMap id="MailTemplateResultMap" type="com.miniproject.mail.domain.MailTemplate">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="fromAddress" column="from_address"/>
        <result property="fromName" column="from_name"/>
        <result property="toAddresses" column="to_addresses"/>
        <result property="ccAddresses" column="cc_addresses"/>
        <result property="bccAddresses" column="bcc_addresses"/>
        <result property="subject" column="subject"/>
        <result property="body" column="body"/>
        <result property="html" column="html"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="MailTemplateColumns">
        id, code, name, description, from_address, from_name, to_addresses, cc_addresses,
        bcc_addresses, subject, body, html, enabled, created_at, updated_at
    </sql>

    <select id="findById" resultMap="MailTemplateResultMap">
        SELECT <include refid="MailTemplateColumns"/>
        FROM pjt_mail_templates
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="MailTemplateResultMap">
        SELECT <include refid="MailTemplateColumns"/>
        FROM pjt_mail_templates
        WHERE code = #{code}
    </select>

    <select id="existsByCode" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM pjt_mail_templates WHERE code = #{code})
    </select>

    <select id="findAllByOrderByIdAsc" resultMap="MailTemplateResultMap">
        SELECT <include refid="MailTemplateColumns"/>
        FROM pjt_mail_templates
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO pjt_mail_templates (
            code, name, description, from_address, from_name, to_addresses, cc_addresses,
            bcc_addresses, subject, body, html, enabled, created_at, updated_at
        ) VALUES (
            #{code}, #{name}, #{description}, #{fromAddress}, #{fromName}, #{toAddresses}, #{ccAddresses},
            #{bccAddresses}, #{subject}, #{body}, #{html}, #{enabled}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update">
        UPDATE pjt_mail_templates
        SET name = #{name},
            description = #{description},
            from_address = #{fromAddress},
            from_name = #{fromName},
            to_addresses = #{toAddresses},
            cc_addresses = #{ccAddresses},
            bcc_addresses = #{bccAddresses},
            subject = #{subject},
            body = #{body},
            html = #{html},
            enabled = #{enabled},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM pjt_mail_templates WHERE id = #{id}
    </delete>

</mapper>
